//Math = built-in object that provides a 
//      collection of properties and methods

let x = 3;
let y = 2;
let z = 9.11;


let a = 10.676767777676;
let b = 33;
let c = 36;
let d = 54;
let e = 56;
let f = 43;
let g = -88;
let h = 12;


x = Math.round(x);
y = Math.floor(y);
z = Math.ceil(z);
a = Math.trunc(a);
b = Math.pow(x,y);
c = Math.sqrt(c);
d = Math.sin(d);
e = Math.cos(e);
f = Math.tan(f);
g = Math.abs(g);
h = Math.sign(h);
let max = Math.max(x,y,z);
let min = Math.min(x,y,z);


console.log(x, y, z, a, b, c, d, e, f, g, h);

//round = normally you round the numbers from 1-5 down and from 5-10 up.
//floor = same, but only round down, not up.
//ceil = same, but only up, not down
//trunc = it cuts off the decimal like instead of this 65.23, it transforms to only 65.
//pow = meaning like x^y like u see when x = 3, y = 2. the x^y is like 3*3, so its 9. like 2 ** 2;
//sqrt = square root 
/*

sin, cos, tan = will be learned later like grade 9/10th.

*/



/*abs = meaning absolute value, so it means for example, 
    -5 will be 5 and also 5 will be just 5 
    because the absolute value wants the negative number 
    to be positive, even if it 0, its still 0, so -1 --> 1 and 1 --> 1.
*/

//sign = positive --> 1, negative --> -1, zero --> 0.

//max = highest number
//min = lowest number

//That's it!